*.o
*.o.d
*.cmd
*.mod
!go.mod
*.ko
*.mod.c
Module.symvers
modules.order

# python / opencode tooling artifacts
__pycache__/
*.pyc

# local-only IPMI credentials (NEVER commit) and editor config
.ipmi.creds
.vscode/

# local QEMU build backups
qemu-backup/
*.qemu-bundle.tar.gz

# built Go MCP binary (source lives in .opencode/mcp-go/)
.opencode/mcp/shannon-mcp

# IPMI credentials (never commit)
.ipmi.creds*
